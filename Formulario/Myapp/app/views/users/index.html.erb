<em><h1 align="center">USUARIOS</h1></em>

<table align="center">
  <tr>
    <th>Nombre</th>
    <th>Apellido</th>
    <th>Teléfono</th>
    <th>E-mail</th>
  </tr>

  <% @users.each do |user| %>
  <tr>
    <td><%= user.name %></td>
    <td><%= user.lastname %></td>
    <td><%= user.phone %></td>
    <td><%= user.email %></td>
    <td><%= link_to "Editar Datos", edit_user_path(user) %></td>
    <td><%= link_to "Agregar Nuevo Usuario", new_user_path(user) %></td>
    <td><%= link_to "Eliminar Dato", user_path(user) ,method: :delete, data:{confirm: "Estas Seguro?"} %></td>
  </tr>
  <% end %>
</table>